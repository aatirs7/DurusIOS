import { Screen } from "@/components/Screen";
import { Text } from "@/components/Text";

/* Placeholder. See the plan's sequence: drills come after the data layer. */
export default function Placeholder() {
  return (
    <Screen>
      <Text variant="pageTitle">cases</Text>
    </Screen>
  );
}
